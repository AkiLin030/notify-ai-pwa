{
  "name": "notify-backend",
  "version": "1.0.0",
  "description": "Backend for AI Reminder PWA",
  "main": "src/api.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/client-sns": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.1053.0",
    "@aws-sdk/util-dynamodb": "^3.0.0",
    "web-push": "^3.6.7"
  }
}
